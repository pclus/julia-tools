] add Example # will install a recoverable version of the package (like a release)
] dev Example # will use the state of the package in the path, even if this changes
] free Example # will revert the `dev Example`
